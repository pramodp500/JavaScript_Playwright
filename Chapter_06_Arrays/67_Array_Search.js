let results = ["pass", "fail", "pass", "skip", "pass", "fail"];
results.indexOf("fail")


results.lastIndexOf("fail");

results.includes("skip");

let nums = [10, 25, 30, 44];

nums.find(x => x > 20);

nums.findIndex(x => x > 20);

nums.findLast(x => x > 20);

nums.findLastIndex(x => x > 20);